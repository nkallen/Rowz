- Move shardRepo config into gizzard:
  shardRepository        += ("com.state.rowz.SqlShard"            -> new SqlShardFactory(queryEvaluatorFactory, config))
  shardRepository        += ("com.state.gizzard.ReadOnlyShard"    -> new gizzard.ReadOnlyShardFactory)
  shardRepository        += ("com.state.gizzard.BlockedShard"     -> new gizzard.BlockedShardFactory)
- Make ThrottledLogger, etc. part of Gizzard
